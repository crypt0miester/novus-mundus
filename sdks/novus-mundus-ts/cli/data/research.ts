/**
 * Research Template Data — re-exported from the SDK research catalog.
 *
 * The catalog (src/data/research-catalog.ts) is the single source of truth
 * shared by this init CLI and the web UI. Edit it there, not here.
 */

export {
  RESEARCH_CATALOG as RESEARCH_TEMPLATES,
  type ResearchNode as ResearchTemplateData,
} from '../../src/data/research-catalog';
