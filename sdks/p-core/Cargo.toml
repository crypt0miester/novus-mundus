[package]
name = "p-core"
version = "0.2.3"
edition = "2021"

[dependencies]
pinocchio = { version = "0.10.2", features = ["cpi"] }
solana-address = { version = "2", features = ["decode"] }

[features]
default = []

[lib]
crate-type = ["lib"]
name = "p_core"
