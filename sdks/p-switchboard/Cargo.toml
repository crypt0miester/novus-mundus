[package]
name = "p-switchboard"
version = "0.2.0"
edition = "2021"
description = "pinocchio/no_std port of the Switchboard On-Demand SDK (PullFeedAccountData)"
license = "MIT"

[lib]
crate-type = ["lib"]

[features]
default = []
no-panic-handler = []

[dependencies]
pinocchio = "0.10.2"
